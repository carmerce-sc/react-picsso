import { numStr } from "../../types";

type PicssoMarginConfigType = {
  margin?: numStr;
  m?: numStr;
  marginX?: numStr;
  mx?: numStr;
  marginY?: numStr;
  my?: numStr;
  marginTop?: numStr;
  mt?: numStr;
  marginRight?: numStr;
  mr?: numStr;
  marginBottom?: numStr;
  mb?: numStr;
  marginLeft?: numStr;
  ml?: numStr;
};

export default PicssoMarginConfigType;

import { numStr } from "./common";

export type PicssoMarginConfigType = {
  margin?: numStr;
  m?: numStr;
  marginX?: numStr;
  mx?: numStr;
  marginY?: numStr;
  my?: numStr;
  marginTop?: numStr;
  mt?: numStr;
  marginRight?: numStr;
  mr?: numStr;
  marginBottom?: numStr;
  mb?: numStr;
  marginLeft?: numStr;
  ml?: numStr;
};
